# 上位8タイル固定時の下位タイル再構築に関する完全解析仕様

## 1. 目的

上位タイルが直線型に配置された場合と、2×2型に配置された場合について、
上位8タイルを一切動かさず、残り8マスから非保護の128タイルを新たに作れる
確率を完全探索で比較する。この差を、両配置で2枚目の32768作成成功率が異なる
理由を説明する一つの指標として用いる。

本解析は、次の離散的な到達性に加えて、標準spawn分布における最適方策下の
成功確率を求める。

- 成功する操作・spawn列が少なくとも一つ存在するか
- どのspawn結果に対しても成功を保証する方策が存在するか
- 成功に必要な最短手数と最少spawn数
- 成功を再現する操作列、spawn位置、spawn値
- 各中間タイルへの到達可能性と保証到達可能性
- 制約下で合法手が存在しない詰み状態
- 保護タイル移動予算を変更した場合の差

## 2. 配置条件

既存研究の「直線型」「2×2型」は上位8タイル全体ではなく、上位3タイルの
位置による分類である。

- 直線型: 上位3タイルが同じ行または同じ列にある
- 2×2型: 上位3タイルが同一の2×2領域内にある

現在の代表配置は、8タプル・ターン14500の標本に対する順位とセルの
最大重み一対一割当から得た次の配置である。セル番号はrow-majorの0～15。

| 条件 | rank 1から8のセル |
|---|---|
| 直線型 | `0,1,2,3,7,6,5,4` |
| 2×2型 | `0,4,1,5,2,9,6,7` |

両条件でタイル値の多重集合を同一にし、幾何形状だけを比較する。

| rank | 値 | 指数表現 |
|---:|---:|---:|
| 1 | 16384 | 14 |
| 2 | 8192 | 13 |
| 3 | 4096 | 12 |
| 4 | 2048 | 11 |
| 5 | 1024 | 10 |
| 6 | 512 | 9 |
| 7 | 256 | 8 |
| 8 | 128 | 7 |

この盤面は因果比較用の反実仮想盤面である。ターン14500標本から順位別の配置を
取得する一方、タイル値の多重集合は全配置で上記の16K～128へ統一している。

## 3. 2048の遷移規則

タイル値は指数で保持する。空セルは0、2は1、4は2、128は7、16384は14とする。

1. ゲーム開始前に、8つの自由セルの異なる2セルへタイルを2枚spawnする。
2. 各spawnの値は2または4とし、位置と値の全組合せを列挙する。
3. プレイヤーは上、右、下、左のいずれかへ通常の2048スワイプを行う。
4. 同じスワイプ中に一度結合したタイルは再結合しない。
5. 盤面が変化しないスワイプは合法手としない。
6. 目標未達なら、スワイプ後の空きセル1個へ2または4をspawnする。
7. 非保護128が完成した直後を成功終端とし、その後のspawnは行わない。

通常の確率90%（2）・10%（4）は、完全到達解析では使用しない。2と4を
確率事象ではなく、列挙対象となる非決定的な遷移として扱う。

## 4. 保護タイル規則

初期上位8タイルには保護タグを付け、同値の非保護タイルと区別する。

現在実装済みの移動予算は0である。通常の2048スワイプを適用した結果、
次のいずれかが起きる手は制約違反として除外する。

- 保護タイルが初期セルから移動する
- 保護タイルの値が変化する
- 保護タイルが別タイルと結合する
- 保護タグが自由セルへ移る

保護タイルを「障害物として独自に固定したスライド規則」にはしない。
必ず標準2048のスワイプを盤面全体へ適用した後、その手が保護条件を満たすか
判定する。

移動予算1、2は未実装である。追加時は各保護タイルの同一性と位置を状態へ
含め、全保護タイルが1手で移動したマンハッタン距離の累積をコストとする。
保護タイルの結合は、移動予算にかかわらず失敗扱いとする。

## 5. 成功条件

保護タグを持たないタイルとして128（指数7）が生成された時点で成功とする。
初期rank 8の保護128は成功判定に含めず、新しい128と保護128を実際に結合する
ことも成功条件に含めない。これは「残り8マスで128系列を再構築する能力」と、
その後に既存系列へ接続する操作能力を分離するためである。

## 6. 完全解析の意味

### 6.1 可能到達

プレイヤーの操作とspawn位置・spawn値の双方を選択可能として扱う。
初期状態から目標状態への経路が一つでもあれば「可能」とする。

幅優先探索を用いる。初期spawnは2回、その後は「1手＋1spawn」を1辺とし、
成功手ではspawnを行わない。このため最初に発見した成功経路は最短手数であり、
同時に最少spawn数となる。

128を全て4から作る場合、最短spawn数の理論下限は32である。

### 6.2 保証到達

プレイヤー操作をOR節、spawn位置・値をAND節とする到達ゲームとして扱う。

状態`S`を勝ち状態とする条件は、次を満たす合法手が少なくとも一つあること。

- その手で直接目標へ到達する、または
- その手の後に起こり得る全spawn後状態が既に勝ち状態である

目標直前状態から最小不動点を反復計算する。初期2回のspawnについても全結果が
勝ち状態である場合に限り、初期状態からの成功を「保証可能」とする。

保証解析は、到達可能状態の列挙が状態数・時間上限で打ち切られず完了した場合だけ
実行できる。不完全な状態集合から保証不能という結論を出してはならない。

### 6.3 最適方策下の到達確率

`probability`モードでは、spawn位置をmove後の空きセル上の一様分布、spawn値を
2が90%、4が10%として、プレイヤーが目標到達確率を最大にする場合の理論成功率を
求める。これは既存AIの実測成功率、ランダム操作の成功率、到達状態に占める成功状態
の割合ではない。

状態`s`における値を`V(s)`とすると、各合法手について全spawn後状態の確率加重平均を
求め、その最大値を`V(s)`とする。move直後に目標へ到達する手の値は1、合法手がなく
目標未達の状態は0とする。初期2回のspawnは、1回目を8セル、2回目を残る7セルから
逐次一様に選び、値の90%/10%を掛けて初期値を平均する。

moveは自由タイルの値の総和を保存し、非終端遷移ではその後のspawnによって総和が
必ず2または4増える。このため状態グラフは総和順のDAGであり、総和の大きい状態から
一度ずつ処理する動的計画法で無限期間の最適到達確率を計算できる。反復収束は不要で
ある。浮動小数の推定値に加えて、`10^15`を尺度とする固定小数点整数を外向きに丸め、
真値を必ず含む下限・上限と区間幅を出力する。

## 7. 状態表現

移動予算0では保護8セルは不変なので、自由8セルだけを各4bitで符号化する。
盤面状態は合計32bitである。

現在の128解析では指数7を成功終端として状態表へ保存しないため、各自由セルに
保存される値は0～6の7通りである。非終端状態の単純な理論上限は次のとおり。

```text
7^8 = 5,764,801 states
```

現在の試作は高速なnibble符号化を使うため、32bitのアドレス空間
（最大`2^32`コード）を利用する。dense版ではbase-7符号化により上記上限へ
縮小できる。

現在の試作実装は`unordered_map`と親ノード配列を使用する。正当性検証と速度測定には
適するが、全32bit空間の本番解析にはメモリ効率が不足する。本番版では次を検討する。

- 状態IDを直接添字にしたdense bitset
- 32bit親状態と8bit操作情報の分離保存
- frontier単位の外部メモリまたはノード分割
- 4台へのlayout・移動予算別の独立割当
- 必要ならMPIによる状態ID範囲分割

## 8. 出力

標準出力へ次を出す。

- 配置、目標指数、モード
- 発見状態数、完全列挙の成否
- 経過時間、状態生成速度
- 詰み状態数
- 可能到達の真偽
- 最短手数、最少spawn数
- 2、4、8、…、目標までの初回到達手数・spawn数
- 保証到達の真偽と勝ち状態数（保証モードのみ）
- 最適成功確率、認証下限・上限、区間幅（確率モードのみ）

`--witness-output`指定時はCSVへ次を出す。

```text
step,move,spawn_cell,spawn_value,state_code
```

初期2行はmoveが`-`であり、2回の初期spawnを表す。成功最終手はspawnなしである。

## 9. 実行モードと完全性

- `witness`: 最初の成功経路を発見した時点で停止する。存在証拠は厳密だが、
  未発見時に不可能とは結論できない。
- `enumerate`: 到達可能な非目標状態を全列挙する。打切りなしで終了した場合のみ、
  未発見を到達不可能の証明とみなせる。
- `guarantee`: 完全列挙後にAND/OR不動点を計算する。
- `probability`: 完全列挙後、標準spawn確率と最適プレイヤー方策の下で到達確率を計算する。

`--max-states`または`--max-seconds`へ到達した場合は`complete=false`とし、
未発見時の`possible`は`unknown`、最大到達値は下界として出力する。
不可能・保証不能の証明には使用しない。

## 10. 現在の実装範囲と今後の追加順

現在の試作で実装する範囲:

1. 代表直線型・代表2×2型
2. 通常2048のmoveと2/4 spawn全列挙
3. 保護移動予算0
4. 可能到達BFS、最短証拠、マイルストーン、詰み数
5. 完全列挙後の保証到達不動点
6. 状態数・時間上限と速度測定
7. 標準spawn分布における最適方策下の到達確率と認証区間

速度測定後の追加候補:

1. dense状態表現
2. OpenMP並列frontier
3. チェックポイントと再開
4. 4台分散
5. 保護移動予算1、2
6. 2枚の32768を実際に結合する成功条件
7. first-32768実在盤面を入力するモード

追加順は、小目標で測定した状態生成速度、重複率、frontier最大幅、1状態当たり
メモリ量に基づいて決める。
